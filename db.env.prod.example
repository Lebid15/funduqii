# Funduqii PostgreSQL PRODUCTION env (EXAMPLE) — copy to db.env.prod (NOT committed).
# Must match the credentials in DATABASE_URL (backend.env.prod).
POSTGRES_DB=funduqii
POSTGRES_USER=funduqii
POSTGRES_PASSWORD=CHANGE_ME_STRONG_DB_PASSWORD
