# Core runtime dependencies (all environments).
Django>=5.1,<5.2
djangorestframework>=3.15,<3.16
djangorestframework-simplejwt>=5.3,<6.0
django-environ>=0.11,<0.13
django-cors-headers>=4.4,<5.0
psycopg[binary]>=3.2,<4.0

# Scalability / performance / realtime foundation (Phase 1.5)
redis>=5.0,<6.0
celery>=5.4,<6.0
channels>=4.1,<5.0
channels-redis>=4.2,<5.0
# ASGI server for Channels (serves WebSockets; also required by channels.testing).
daphne>=4.1,<5.0
